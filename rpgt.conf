# This file is written in TOML format (see https://toml.io/en/)

[rpgt]
# Directory where the characters are stored
char_dir = "./characters"

# Directories where the modules are stored
mod_dirs = ["./modules"]